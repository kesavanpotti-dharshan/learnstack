---
title: Introduction
sidebar_position: 1
slug: /
---

export const topics = [
  {
    emoji: "🧩",
    title: ".NET",
    desc: "C#, Clean Architecture, DDD/CQRS, and .NET internals.",
    link: "/dotnet",
  },
  {
    emoji: "☁️",
    title: "Azure",
    desc: "AKS, Service Bus, Functions, Entra ID, and cloud architecture notes.",
    link: "/azure",
  },
  {
    emoji: "⚛️",
    title: "React",
    desc: "React, TypeScript, hooks, and frontend patterns.",
    link: "/react",
  },
  {
    emoji: "🛠️",
    title: "DevOps",
    desc: "CI/CD, containers, Kubernetes, and infrastructure as code.",
    link: "/devops",
  },
  {
    emoji: "🧮",
    title: "DSA",
    desc: "Data structures, algorithms, and problem-solving patterns.",
    link: "/dsa",
  },
  {
    emoji: "🏗️",
    title: "System Design",
    desc: "Scalability, distributed systems, and design trade-offs.",
    link: "/system-design",
  },
];

export const TopicCard = ({ emoji, title, desc, link }) => (
  <a
    href={link}
    style={{
      display: "block",
      textDecoration: "none",
      color: "inherit",
      border: "1px solid var(--ifm-color-emphasis-300)",
      borderRadius: "12px",
      padding: "1.5rem",
      height: "100%",
      transition:
        "transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease",
      background: "var(--ifm-background-surface-color)",
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.transform = "translateY(-4px)";
      e.currentTarget.style.boxShadow = "0 8px 20px rgba(0,0,0,0.12)";
      e.currentTarget.style.borderColor = "var(--ifm-color-primary)";
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.transform = "translateY(0)";
      e.currentTarget.style.boxShadow = "none";
      e.currentTarget.style.borderColor = "var(--ifm-color-emphasis-300)";
    }}
  >
    <div style={{ fontSize: "2rem", marginBottom: "0.5rem" }}>{emoji}</div>
    <h3 style={{ margin: "0 0 0.5rem 0" }}>{title}</h3>
    <p
      style={{
        margin: 0,
        color: "var(--ifm-color-emphasis-700)",
        fontSize: "0.95rem",
      }}
    >
      {desc}
    </p>
    <div
      style={{
        marginTop: "1rem",
        fontWeight: 600,
        color: "var(--ifm-color-primary)",
        fontSize: "0.9rem",
      }}
    >
      Explore →
    </div>
  </a>
);

<div
  style={{
    textAlign: "center",
    padding: "2.5rem 1rem",
    marginBottom: "2rem",
    borderRadius: "16px",
    background:
      "linear-gradient(135deg, var(--ifm-color-primary) 0%, var(--ifm-color-primary-darker) 100%)",
    color: "white",
  }}
>
  <div style={{ fontSize: "2.75rem", marginBottom: "0.5rem" }}>📚</div>
  <h1 style={{ color: "white", margin: "0 0 0.75rem 0", fontSize: "2.25rem" }}>
    Learnstack
  </h1>
  <p
    style={{
      margin: "0 auto",
      maxWidth: "560px",
      fontSize: "1.05rem",
      opacity: 0.95,
    }}
  >
    Personal learning notes across .NET, Azure, React, DevOps, and more —
    written to revisit before interviews and shared with fellow devs.
  </p>
</div>

<div
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(240px, 1fr))",
    gap: "1.25rem",
    marginBottom: "2.5rem",
  }}
>
  {topics.map((t) => (
    <TopicCard key={t.title} {...t} />
  ))}
</div>

---

### Why this site exists

These are living notes — revisited before interviews, refined after every real-world lesson, and shared so other devs can learn alongside me.
