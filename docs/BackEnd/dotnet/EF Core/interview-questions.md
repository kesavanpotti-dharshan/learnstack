---
title: Interview Questions
sidebar_label: Interview Questions
sidebar_position: 2
---

DbContext thread safety and concurrency in EF Core

How to use IDbContextFactory for background tasks

EF Core Change Tracker states added modified deleted

Avoiding memory leaks with long-lived DbContext

How SaveChanges detects changes and updates database
how to scaffold dbcontext in ef core command line

handling database migrations with ef core in production

ef core code first best practices and folder structure

dealing with schema drift in ef core database first

entity framework core performance tuning tips
EF Core IEntityTypeConfiguration example

configure one to many relationship fluent api ef core

ef core vs data annotations comparison

configure composite primary key fluent api

ef core ignore property fluent api
when to use TPH vs TPT vs TPC in EF Core

performance implications of entity splitting vs table splitting

how to query derived types in TPC and TPT

handling required vs optional properties in table splitting

migrating from TPT to TPC in Entity Framework Core
EF Core AsNoTracking vs Tracking with loading strategies

how to avoid N+1 query problem in Entity Framework Core

EF Core projection vs eager loading performance comparison

handling circular references in EF Core json serialization

how to implement split queries for large collections in EF Core
EF Core Split Queries vs Single Query performance

How to enable EF Core query command logging and interceptors

Using AsSplitQuery for large collection loading

EF Core AsNoTracking performance benefits and best practices

Entity Framework Core lazy loading dangers and best practices
how to handle nested collections in ef core projections

ef core select vs asnotracking performance comparison

updating data using ef core select projections

how to project into existing objects in ef core

ef core compiled queries with select projections
When should you avoid split queries in EF Core

How does AsSplitQuery affect database transactions

EF Core Select vs Include performance comparison

How to change the default query behavior globally

Debugging SQL generation and execution in EF Core
EF Core ExecuteUpdate vs ExecuteDelete performance benchmarks

Using EFCore.BulkExtensions for bulk inserts and updates

How SQL Server handles command batching in EF Core

Avoiding N+1 queries with AsSplitQuery and batching

Writing efficient set-based bulk operations in EF Core 8
How to handle DbUpdateConcurrencyException in EF Core

Using IsConcurrencyToken vs IsRowVersion in EF Core

EF Core optimistic concurrency with alternate keys

Implementing atomic conditional updates in EF Core

Handling database transactions in EF Core apps
